# TOPIK 마스터 — 프로젝트 작업 규칙

## 자동 커밋 & 푸시 (사용자 상시 승인됨)

하나의 논리적 작업이 끝날 때마다 아래 3단계를 **자동으로** 실행하고, 과정 설명은 생략한 채 **결과만 간결히 보고**한다. (이 규칙은 사용자가 명시적으로 상시 승인한 것이며, 매번 별도 확인을 받지 않는다.)

```bash
git add .                              # 저장소 루트에서 실행
git commit -m "<작업 내용 한 줄 요약>"
git push origin main
```

### 세부 규칙

- **커밋 메시지**: 작업 내용을 영어 명령형(imperative)으로 한 줄, 명확하게 작성한다.
  - 예: `Add login form validation` · `Fix CORS error in backend` · `Add weekly leaderboard endpoint`
- **실행 시점**: 기능 추가·버그 수정·리팩터링 등 **코드/파일 변경이 완료된 시점**에만 실행한다.
  - 단순 질문·설명에 답하기만 한 경우, 또는 작업 도중 확인차 멈춘 경우에는 실행하지 않는다.
- **변경 없음 처리**: 스테이징할 변경이 없으면 커밋·푸시를 건너뛴다 (빈 커밋 금지).
- **푸시 대상**: `origin main` — `https://github.com/nooruapp-ai/topik-master.git`
- **보고 형식**: `짧은 커밋 해시 · 커밋 메시지 · 푸시 결과(브랜치/원격)` 만 한두 줄로 보고한다.
- **실패 처리**: 커밋·푸시가 실패하면 "결과만"으로 끝내지 말고 **에러 원인과 해결 방법**을 알린다.
- **보안**: `git add .` 전에 `.env` 등 민감 파일이 `.gitignore`로 제외되어 있는지 신뢰한다. 새 비밀 파일이 보이면 커밋하지 말고 먼저 알린다.

### 참고
- 이 규칙은 Claude가 따르는 **작업 지침**이다(하네스가 강제하는 훅이 아님). 따라서 "요약 커밋 메시지"를 사람이 읽기 좋게 자동 생성할 수 있다.
